# Sprint 14 Contract — 卡片渲染解耦与多形态 Skill

## 成功标准

1. 存在独立的 `render_card()` Tool
2. `load_skill()` 与 card 渲染职责边界清晰，避免继续耦合
3. 前端不再只依赖 `referral / exercise` 二分渲染
4. 至少有 1-2 种新增 card 形态可以稳定展示
5. 现有 referral / exercise 卡片不回退
